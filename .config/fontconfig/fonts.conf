<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <its:rules xmlns:its="http://www.w3.org/2005/11/its" version="1.0">
    <its:translateRule translate="no" selector="/fontconfig/*[not(self::description)]"/>
  </its:rules>

  <description>Optimized Font Configuration for Professional Display — with Maple Mono (fixed autohint, deduplicated, Segoe UI first)</description>

  <dir>/usr/share/fonts</dir>
  <dir>~/.local/share/fonts</dir>
  <dir prefix="xdg">fonts</dir>

  <!-- 全局关闭嵌入式位图 -->
  <match target="font">
    <edit name="embeddedbitmap" mode="assign"><bool>false</bool></edit>
  </match>

  <!--
    核心渲染参数
    - autohint=false: 使用字体自带的 BCI hinting（对微软/Noto 等大厂字体效果远好于自动推测）
    - hintstyle=hintslight: 保留字形轮廓的同时获得不错的清晰度
    - lcddefault: 标准 LCD 滤镜，减少色边
  -->
  <match target="font">
    <edit name="antialias" mode="assign"><bool>true</bool></edit>
    <edit name="hinting" mode="assign"><bool>true</bool></edit>
    <edit name="hintstyle" mode="assign"><const>hintslight</const></edit>
    <edit name="rgba" mode="assign"><const>rgb</const></edit>
    <edit name="lcdfilter" mode="assign"><const>lcddefault</const></edit>
    <edit name="autohint" mode="assign"><bool>false</bool></edit>
  </match>

  <!--
    权重修正：仅将 thin/extralight 垫高到 regular
    保留 regular / medium / bold 的原始设计层次
  -->
  <match target="pattern">
    <test name="weight" compare="less"><const>regular</const></test>
    <edit name="weight" mode="assign"><const>regular</const></edit>
  </match>

  <!-- ════════════════════════════════════════════════════════
       字体 Fallback 链（alias 方式，单一权威来源）
       ════════════════════════════════════════════════════════ -->

  <!-- Serif -->
  <alias>
    <family>serif</family>
    <prefer>
      <family>Noto Serif</family>
      <family>Noto Serif CJK SC</family>
      <family>Source Han Serif CN</family>
    </prefer>
  </alias>

  <alias>
    <family>sans-serif</family>
    <prefer>
      <family>Fira Sans</family>
      <family>Segoe UI</family>
      <family>Roboto</family>
      <family>Noto Sans CJK SC</family>
      <family>Source Han Sans CN</family>
    </prefer>
  </alias>

  <!-- Monospace -->
  <alias>
    <family>monospace</family>
    <prefer>
      <family>Maple Mono NF CN</family>
      <family>Maple Mono CN</family>
      <family>DejaVu Sans Mono</family>
      <family>Noto Sans Mono CJK SC</family>
    </prefer>
  </alias>

  <!-- Maple Mono 自身缺字回退 -->
  <alias>
    <family>Maple Mono CN</family>
    <default><family>monospace</family></default>
  </alias>
  <alias>
    <family>Maple Mono NF CN</family>
    <default><family>monospace</family></default>
  </alias>

  <!-- Emoji & Math -->
  <alias>
    <family>Apple Color Emoji</family>
    <prefer><family>Noto Color Emoji</family></prefer>
  </alias>
  <alias>
    <family>Segoe UI Emoji</family>
    <prefer><family>Noto Color Emoji</family></prefer>
  </alias>
  <match>
    <test name="family"><string>emoji</string></test>
    <edit name="family" mode="prepend" binding="strong"><string>Noto Color Emoji</string></edit>
  </match>
  <match>
    <test name="family"><string>math</string></test>
    <edit name="family" mode="prepend" binding="strong"><string>Latin Modern Math</string></edit>
  </match>

  <!-- 缓存目录 -->
  <cachedir>/var/cache/fontconfig</cachedir>
  <cachedir prefix="xdg">fontconfig</cachedir>

  <!-- 每 60 秒重扫 -->
  <config>
    <rescan><int>60</int></rescan>
  </config>
</fontconfig>
